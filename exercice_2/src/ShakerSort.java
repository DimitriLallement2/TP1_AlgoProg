package src;

/**
 * Tri bulle melange
 */
public abstract class ShakerSort extends Sort {

	@Override
	public void sort(int[] tab) {
		//
		// int taille = liste.getTaille();
		// /* On verifie qu il est necessaire de trier */
		// if (taille > 1)
		// {
		// /*
		// On parcourt les elements de la liste 2 par 2 et on les echange de
		// place si besoin
		// */
		// for (int i = 0; i < taille; i++)
		// {
		// for (int j = 0; j < (taille - i - 1); j++) {
		// NoeudForme courant = liste.get(j), suivant = liste.get(j + 1);
		// /* Si la comparaison retourne 1, on inverse les elements */
		// if (comparer(courant.getForme(), suivant.getForme()) > 0) {
		// liste.permutter(courant, suivant);
		// }
		// }
		// }
		// }
		}
	}
